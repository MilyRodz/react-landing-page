import React from 'react';

const Footer = ({fecha}) => (
    <footer className="bg-dark text-muted text-center mt-4 mb-0 p-4"> 
        Copywrite &copy; {fecha}
    </footer>
  );

export default Footer;